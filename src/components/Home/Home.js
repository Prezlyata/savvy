import React from 'react';
import LoginFormContainer from '../LoginForm/LoginFormContainer'

const Home = (props) => {

	return (
		<div>
            <LoginFormContainer />
		</div>
	);
};

export default Home;
